node_modules
dist
build
.DS_Store
.env

# Ignore the output video from Git but not videos you import into src/.
out

# Bundle Remotion compartilhado entre renders (gerado pelo backend).
# Reaproveitado via fingerprint do conteúdo de src/.
.bundle-cache/
